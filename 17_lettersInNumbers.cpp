#include <iostream>
#include <string>
#include <vector>
#include <stdio.h>
#include <climits>
#include <algorithm>

//how many letters needed to spell out first thousand numbers?
// actually dumb brute force problem
int main(int argc, char** argv)
{
  int numLetters = 14;
  for(int i = 2; i < 1000; i++)
  {
    if(i % 100 )
  }
  int c = 230 % 100;
  std::cout << c << '\n';
  return 0;
}
